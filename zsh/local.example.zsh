# Copy snippets from here into ~/.config/dot/*.zsh or zsh/*.local files.
# Do not put real tokens or work-only secrets in tracked files.

# ~/.config/dot/secrets.zsh
# export GITHUB_TOKEN="..."
# export OPENAI_API_KEY="..."
# export ANTHROPIC_API_KEY="..."

# ~/.config/dot/work.zsh
# export VAULT_ADDR="https://vault.example.com"
# alias workvpn="..."

# ~/.config/dot/hosts/my-host.zsh
# path=("$HOME/vendor/bin" $path)
